{
  "name": "dates-picker",
  "version": "1.0.5",
  "description": "⚛️📆 Flexible React date range picker calendar with no dependencies.",
  "private": false,
  "homepage": "https://almogtavor.github.io/date-range-picker/",
  "main": "src/index.js",
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "^4.0.3"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "prettier": "2.2.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "^4.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "rm -rf ./dist && npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/almogtavor/date-range-picker.git"
  }
}
